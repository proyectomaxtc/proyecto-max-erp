name: proyecto_max
description: "Proyecto MAX ERP"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Arquitectura
  flutter_riverpod: ^2.6.1
  go_router: ^17.3.0

  # UI
  google_fonts: ^8.1.0
  flutter_animate: ^4.5.2
  responsive_framework: ^1.5.1

  # Utilidades
  intl: ^0.20.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/